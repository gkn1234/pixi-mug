import Tap from'./Tap.js'

import Game from '@/libs/Game.js'
import utils from '@/libs/utils/index.js'

import NoteUtils from './NoteUtils.js'

class 

export default class Swipe extends Tap {
  constructor () {
  }
  
  // 滑键方向
  static DIRECTION = {
    up: 0,
    right: 1,
    down: 2,
    left: 3
  }
  
  init
}
